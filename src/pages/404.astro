<!doctype html>
<html>
  <head>
    <style>
      body {
        background: #666;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, sans-serif;
      }

      .window {
        background: #fff;
        border: 2px solid #000;
        border-radius: 6px;
        width: 400px;
        box-shadow: 2px 2px 0px #000;
      }

      .title-bar {
        background: repeating-linear-gradient(
          to right,
          #000,
          #000 2px,
          #fff 2px,
          #fff 4px
        );
        height: 24px;
        border-bottom: 2px solid #000;
        display: flex;
        align-items: center;
        padding: 0 8px;
      }

      .title {
        background: white;
        padding: 0 8px;
        font-weight: bold;
        font-size: 14px;
      }

      .content {
        padding: 20px;
        text-align: center;
      }

      .icon {
        font-size: 48px;
        margin-bottom: 16px;
      }

      .message {
        font-size: 14px;
        margin-bottom: 24px;
        line-height: 1.4;
      }

      .button {
        background: white;
        border: 2px solid #000;
        border-radius: 4px;
        padding: 6px 20px;
        font-size: 14px;
        font-weight: bold;
        cursor: pointer;
      }

      .button:active {
        background: #000;
        color: white;
      }
    </style>
  </head>
  <body>
    <div class="window">
      <div class="title-bar">
        <div class="title">Error 404</div>
      </div>
      <div class="content">
        <div class="icon">&#128193;</div>
        <div class="message">
          The requested resource could not be found.<br /><br />
          Error ID: 404<br />
          "Page not found"
        </div>
        <button class="button" onclick="window.location.href='/'">OK</button>
      </div>
    </div>
  </body>
</html>
