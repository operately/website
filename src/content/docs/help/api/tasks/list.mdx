---
title: "Tasks API: List"
description: "List with the Operately Tasks API."
---

import CurlExampleBlock from "@components/CurlExampleBlock.jsx"

## Endpoint

| Property | Value |
| --- | --- |
| Type | `query` |
| Method | `GET` |
| Path | `/api/external/v1/tasks/list` |
| Handler | `OperatelyWeb.Api.Tasks.List` |

## Authentication

- Requires a valid API token.
- Read-only and full-access tokens can execute this query.


## Inputs

<div style={{ overflowX: "auto" }}>
  <table style={{ minWidth: "56rem" }}>
    <thead>
      <tr>
        <th style={{ whiteSpace: "nowrap" }}>Field</th>
        <th style={{ whiteSpace: "normal" }}>Type</th>
        <th style={{ whiteSpace: "nowrap" }}>Required</th>
        <th style={{ whiteSpace: "nowrap" }}>Nullable</th>
        <th style={{ whiteSpace: "nowrap" }}>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
  <td style={{ whiteSpace: "nowrap" }}><code>project_id</code></td>
  <td style={{ whiteSpace: "normal" }}><code>id</code> primitive (encoded as <code>string</code>)</td>
  <td style={{ whiteSpace: "nowrap" }}>Yes</td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>-</td>
</tr>

    </tbody>
  </table>
</div>


## Outputs

| Field | Type | Required | Nullable |
| --- | --- | --- | --- |
| `tasks` | array of `task` object | Yes | No |


## cURL Example

<CurlExampleBlock client:load command={"curl --request GET \\\n  --url \"https://app.operately.com/api/external/v1/tasks/list?project_id=value\" \\\n  --header \"Authorization: Bearer ${OPERATELY_API_TOKEN}\""} />

## Response Example

```json
{
  "tasks": [
    {
      "assignees": [
        "<person>"
      ],
      "available_statuses": [
        "<task_status>"
      ],
      "comments_count": 123,
      "creator": "<person>",
      "description": "value",
      "due_date": "<contextual_date>",
      "id": "value",
      "inserted_at": "2026-01-01",
      "milestone": "<milestone>",
      "name": "value",
      "permissions": "<project_permissions>",
      "priority": "value",
      "project": "<project>",
      "project_space": "<space>",
      "size": "value",
      "space": "<space>",
      "status": "<task_status>",
      "subscription_list": "<subscription_list>",
      "type": "space",
      "updated_at": "2026-01-01"
    }
  ]
}
```

