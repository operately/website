---
title: "list"
description: "Generated reference for the query endpoint spaces/list."
---

import CurlExampleBlock from "@components/CurlExampleBlock.jsx"

## Endpoint

| Property | Value |
| --- | --- |
| Type | `query` |
| Method | `GET` |
| Path | `/api/external/v1/spaces/list` |
| Handler | `OperatelyWeb.Api.Spaces.List` |

## Authentication

- Requires a valid API token.
- Read-only and full-access tokens can execute this query.


## Inputs

<div style={{ overflowX: "auto" }}>
  <table style={{ minWidth: "56rem" }}>
    <thead>
      <tr>
        <th style={{ whiteSpace: "nowrap" }}>Field</th>
        <th style={{ whiteSpace: "normal" }}>Type</th>
        <th style={{ whiteSpace: "nowrap" }}>Required</th>
        <th style={{ whiteSpace: "nowrap" }}>Nullable</th>
        <th style={{ whiteSpace: "nowrap" }}>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
  <td style={{ whiteSpace: "nowrap" }}><code>access_level</code></td>
  <td style={{ whiteSpace: "normal" }}><code>access_options</code> enum (<code>no_access</code>, <code>minimal_access</code>, <code>view_access</code>, <code>comment_access</code>, <code>edit_access</code>, <code>admin_access</code>, <code>full_access</code>)</td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>-</td>
</tr>

<tr>
  <td style={{ whiteSpace: "nowrap" }}><code>include_access_levels</code></td>
  <td style={{ whiteSpace: "normal" }}><code>boolean</code></td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>-</td>
</tr>

<tr>
  <td style={{ whiteSpace: "nowrap" }}><code>include_members</code></td>
  <td style={{ whiteSpace: "normal" }}><code>boolean</code></td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>-</td>
</tr>

    </tbody>
  </table>
</div>


## Outputs

| Field | Type | Required | Nullable |
| --- | --- | --- | --- |
| `spaces` | array of `space` object | No | Yes |


## cURL Example

<CurlExampleBlock client:load command={"curl --request GET \\\n  --url \"https://app.operately.com/api/external/v1/spaces/list?access_level=no_access&include_access_levels=true&include_members=true\" \\\n  --header \"Authorization: Bearer ${OPERATELY_API_TOKEN}\""} />

## Response Example

```json
{
  "spaces": [
    {
      "access_levels": "<access_levels>",
      "color": "value",
      "icon": "value",
      "id": "value",
      "is_company_space": true,
      "is_member": true,
      "members": [
        "<person>"
      ],
      "mission": "value",
      "name": "value",
      "notifications": [
        "<notification>"
      ],
      "permissions": "<space_permissions>",
      "potential_subscribers": [
        "<subscriber>"
      ],
      "private_space": true,
      "task_statuses": [
        "<task_status>"
      ],
      "tasks_kanban_state": "value"
    }
  ]
}
```

