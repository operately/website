---
title: "subscribe_to_notifications"
description: "Generated reference for the mutation endpoint subscribe_to_notifications."
---

## Endpoint

| Property | Value |
| --- | --- |
| Type | `mutation` |
| Method | `POST` |
| Path | `/api/external/v1/subscribe_to_notifications` |
| Handler | `OperatelyWeb.Api.Mutations.SubscribeToNotifications` |

## Authentication

- Requires a valid API token.
- Requires a write-enabled token.
- Read-only tokens return `403`.


## Inputs

<div style={{ overflowX: "auto" }}>
  <table style={{ minWidth: "56rem" }}>
    <thead>
      <tr>
        <th style={{ whiteSpace: "nowrap" }}>Field</th>
        <th style={{ whiteSpace: "normal" }}>Type</th>
        <th style={{ whiteSpace: "nowrap" }}>Required</th>
        <th style={{ whiteSpace: "nowrap" }}>Nullable</th>
        <th style={{ whiteSpace: "nowrap" }}>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
  <td style={{ whiteSpace: "nowrap" }}><code>id</code></td>
  <td style={{ whiteSpace: "normal" }}><code>id</code> primitive (encoded as <code>string</code>)</td>
  <td style={{ whiteSpace: "nowrap" }}>Yes</td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>-</td>
</tr>

<tr>
  <td style={{ whiteSpace: "nowrap" }}><code>type</code></td>
  <td style={{ whiteSpace: "normal" }}><code>subscription_parent_type</code> enum (<code>project_check_in</code>, <code>project_retrospective</code>, <code>goal_update</code>, <code>message</code>, <code>resource_hub_document</code>, <code>resource_hub_file</code>, <code>resource_hub_link</code>, <code>comment_thread</code>, <code>project</code>, <code>milestone</code>, <code>project_task</code>)</td>
  <td style={{ whiteSpace: "nowrap" }}>Yes</td>
  <td style={{ whiteSpace: "nowrap" }}>No</td>
  <td style={{ whiteSpace: "nowrap" }}>-</td>
</tr>

    </tbody>
  </table>
</div>


## Outputs

This endpoint has no output fields.
